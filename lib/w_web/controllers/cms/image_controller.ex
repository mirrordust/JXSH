defmodule WWeb.CMS.ImageController do
  use WWeb, :controller

  alias W.CMS
  alias W.CMS.Image

  def index(conn, _) do
  end

  def show(conn, _) do
  end

  def create(conn, _) do
  end

  def update(conn, _) do
  end

  def delete(conn, _) do
  end
end
